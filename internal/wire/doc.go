// Package wire contains internal codec, registry, framing, TLV, and preface
// helpers used by the public zmux package. These helpers intentionally stay
// behind internal package boundaries so protocol implementation details do not
// become part of the repository's public API surface.
package wire
